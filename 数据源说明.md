# eshop-datasource — 电商交易流模拟数据源

面向 **Flink CDC → Paimon → Flink ETL → Doris → DataEase** 学习链路的 MySQL 数据发生器。
持续、可控地制造三类事件：**大量 insert（下单）+ 中等 update（状态流转/调价）+ 少量 delete（清理）**，
专门用于验证 Flink SQL 窗口函数、Paimon upsert、CDC 更新与删除捕获、事实快照 vs 维表变更等特性。

- 语言/框架：NestJS + TypeScript + mysql2
- MySQL：默认 `localhost:3307`，`root/123456`，库名 `eshop`，时区 `+08:00`（均可通过环境变量覆盖）
- 建库建表：启动时自动执行 `sql/schema.sql`（幂等），可用 `RESET_ON_START=true` 清库重来

---

## 一、表结构（4 张表，见 `sql/schema.sql`）

| 表 | 角色 | 谁在改它 | 产生的 CDC 事件 |
|---|---|---|---|
| `dim_user` | 用户维表 | 用户更新器改字段/注销；清理器硬删 | update / delete |
| `dim_product` | 商品维表（`price` 为当前售价，会变） | 调价器 ±10%、下架；下单扣库存 | update / delete |
| `fact_order` | 订单主表（状态机主战场） | 下单 insert；状态流转器 update；取消器 update；清理器 delete | insert / update / delete |
| `fact_order_item` | 订单明细（**价格快照**，落库后不变） | 只 insert，随订单级联 delete | insert / delete |

关键设计决策：

1. **订单拆主表 + 明细表**，明细存 `unit_price` 快照 —— 商品改价后历史订单金额不变（快照语义）。
2. **`order_time`（事件时间，下单后不变）与 `update_time`（变更时间）分离** —— 算下单量/GMV 用 `order_time`，算状态变更用 `update_time`，避免订单被状态流转"挪进"错误窗口。
3. **全部有主键** —— Flink CDC 与 Paimon 主键表（upsert）都依赖主键定位同一行。
4. **金额 DECIMAL / 时间 DATETIME(3)** —— 无浮点误差，同秒多单有毫秒区分度（窗口和水印需要）。
5. **硬删除而非软删除** —— 要验证"CDC 能捕获删除"，必须有真实的 `DELETE`。
6. **时区统一** —— mysql2 的 `timezone` 选项只是客户端格式化约定，不会向服务器下发 `SET time_zone`；
   `DatabaseService` 对池中每个新连接显式 `SET time_zone = DB_TZ`，保证服务器生成的
   `NOW()/CURRENT_TIMESTAMP/ON UPDATE` 时间与 App 显式写入的时间使用同一套墙钟（默认 +08:00），
   避免 `order_time`（+08:00）与 `update_time`（UTC 墙钟）同一行出现 8 小时缺口。

---

## 二、内置任务（可独立开关）

| 任务 | 频率(默认) | 行为 | 事件 |
|---|---|---|---|
| 种子化 | 启动时 | 1000 用户、200 商品（8 品类），表为空才写（重启幂等） | insert |
| 历史回填 | 启动时 | 回填最近 12h 订单（仅当 `fact_order` 为空），让窗口/大屏立刻有数据 | insert |
| 下单生成器 | 80~1200ms | 随机用户 + 1~3 商品 + 随机数量，主表+明细同事务；`unit_price` 取当时售价快照；可选扣库存 | insert |
| 速率档位 | 每 2min 切档 | 低/中/高三档随机切换下单间隔，模拟促销峰谷 | — |
| 状态流转器 | 1s | 抽样订单乐观更新：CREATED→PAID(20%)→SHIPPED(15%)→COMPLETED(10%) | update |
| 取消器 | 2s | 随机 ~2% 的 CREATED/PAID 订单 → CANCELLED | update |
| 调价器 | 5s | 随机 3~8 个商品价格 ±10%、库存增减；小概率下架 | update |
| 用户更新器 | 8s | 随机 1~3 个用户改 phone/email/city/user_level；小概率注销 | update |
| 删除清理器 | 10s | CANCELLED 超 2min 的订单连同明细硬删；注销用户/下架商品超 10min 硬删 | **delete** |

> 状态流转用「乐观更新」：`UPDATE ... WHERE order_id=? AND order_status=?`，`affectedRows=1` 才算成功，
> 避免并发任务互相覆盖。

---

## 三、快速开始

```bash
npm install
npm run start        # 启动：自动建库建表 → 种子化 → 回填 → 拉起全部任务
```

启动后你会看到类似日志：

```
[DatabaseService] database "eshop" ready
[SeedService] seeded 1000 users
[SeedService] seeded 200 products (8 categories)
[SeedService] backfilled 21600 historical orders over last 12h, status={...}
[SimService] eshop data source simulator started
[OrderGeneratorService] orders created: 25 (items 47), gear=medium
[StatusFlowService] status flow: +10 this tick, total=65
[DeletionCleanerService] hard-deleted 436 CANCELLED orders -> CDC should emit -D events
```

常用变体：

```bash
RESET_ON_START=true npm run start        # 清库重建再启动（演示重来）
BACKFILL_HOURS=3 BACKFILL_RATE=0.2 npm run start   # 少回填一点
ENABLE_ORDER_GENERATOR=false npm run start         # 只开维表类任务
DELETE_USER_MIN=1 DELETE_PRODUCT_MIN=1 npm run start  # 缩短删除窗口，便于观察 delete 事件
```

批量生成数据：

```bash
npm run generate:changelog:batch:1m # 批量生成100万条数据并附带完整changelog
npm run generate:changelog:batch:10m # 批量生成1000万条数据并附带完整changelog
npm run generate:changelog:batch:100m # 批量生成1亿条数据并附带完整changelog
npm run generate:replay:1m # 增量生成数据，模拟实时场景数据流，速率为100万条/30天
npm run generate:replay:10m # 增量生成数据，模拟实时场景数据流，速率为1000万条/30天
npm run generate:replay:100m # 增量生成数据，模拟实时场景数据流，速率为1亿条/30天
```

---

## 四、控制台接口

| 接口 | 说明 |
|---|---|
| `GET  /api/stats` | 运行统计（计数器、档位、uptime）+ 数据库实时快照（表行数、订单状态分布） |
| `POST /api/gear/:gear` | 手动切换下单速率档位：`low` / `medium` / `high` |
| `POST /api/pause` / `POST /api/resume` | 暂停 / 恢复全部任务 |
| `POST /api/seed` | 幂等补种用户/商品（表为空才写）并刷新缓存 |

---

## 五、环境变量（默认值即本机演示环境）

| 变量 | 默认 | 说明 |
|---|---|---|
| `DB_HOST` / `DB_PORT` / `DB_USER` / `DB_PASSWORD` / `DB_NAME` | localhost / 3307 / root / 123456 / eshop | MySQL 连接 |
| `DB_TZ` | `+08:00` | 统一时区，所有 DATETIME 按此时区落库；同时以 `SET time_zone` 下发到每个 MySQL 会话，保证服务器生成的时间（`NOW()`/`ON UPDATE`）与 App 写入时间一致 |
| `AUTO_SCHEMA` / `RESET_ON_START` | true / false | 自动建表 / 清库重建 |
| `SEED_ON_START` / `SEED_USERS` / `SEED_PRODUCTS` | true / 1000 / 200 | 种子数据 |
| `ENABLE_BACKFILL` / `BACKFILL_HOURS` / `BACKFILL_RATE` / `BACKFILL_MAX_ORDERS` | true / 12 / 0.5 / 50000 | 历史回填 |
| `ENABLE_ORDER_GENERATOR` / `DECREMENT_STOCK` | true / true | 下单生成器 / 扣库存 |
| `ENABLE_STATUS_FLOW` / `STATUS_FLOW_INTERVAL_MS` / `STATUS_FLOW_BATCH` | true / 1000 / 40 | 状态流转 |
| `ENABLE_CANCELER` / `CANCELER_INTERVAL_MS` / `CANCELER_BATCH` / `CANCELER_PROB` | true / 2000 / 50 / 0.02 | 取消器 |
| `ENABLE_PRICE_ADJUSTER` / `PRICE_ADJUSTER_INTERVAL_MS` | true / 5000 | 调价器 |
| `ENABLE_USER_UPDATER` / `USER_UPDATER_INTERVAL_MS` / `USER_UPDATER_BATCH` | true / 8000 / 3 | 用户更新器 |
| `ENABLE_CLEANER` / `CLEANER_INTERVAL_MS` / `DELETE_MAX_BATCH` | true / 10000 / 500 | 删除清理器 |
| `DELETE_CANCELLED_ORDER_MIN` / `DELETE_USER_MIN` / `DELETE_PRODUCT_MIN` | 2 / 10 / 10 | 各类硬删除的超时分钟数 |
| `GEAR_SWITCH_MS` | 120000 | 速率档位自动切换间隔 |
| `PORT` | 3000 | HTTP 控制台端口 |

---

## 六、MySQL CDC 前置条件（已就绪，勿关闭）

```ini
[mysqld]
server-id         = 1
log-bin           = mysql-bin
binlog_format     = ROW
binlog_row_image  = FULL      # 关键：update 带全量前后镜像，Paimon upsert 需要完整行
expire_logs_days  = 7
```

---

## 七、这套数据源能验证什么

| 目标 | 观察点 |
|---|---|
| 窗口函数 | `fact_order.order_time` 做滚动/滑动窗口算每分钟 GMV；`fact_order_item.create_time` 按品类聚合；会话窗口按 `user_id + 5min` 间隔算购买会话；Cumulate 算当日累计 |
| Paimon upsert | `fact_order` 同步后，同一 `order_id` 只有最新状态一条；`dim_product` 只有最新价 |
| CDC 捕获字段修改 | 状态流转改 `order_status`、调价器改 `price`，Flink 里能看到 `+U`/`-U` 事件 |
| CDC 捕获删除 | 清理器硬删订单/用户/商品，CDC 发出 `-D` 事件，Paimon 对应行被删 |
| 快照 vs 维表变更 | 商品改价后 `dim_product.price` 变了，但历史 `fact_order_item.unit_price` 与 GMV 不变（可用 SQL 对比验证） |



---

## 七、目录结构

```
sql/schema.sql                  # 唯一 DDL 来源（建库建表）
src/config.ts                   # 环境变量配置
src/db/database.service.ts      # mysql2 连接池 / 事务 / 批量插入
src/sim/
  seed-data.ts                  # 品类/品牌/省市 种子数据
  util.ts                       # 随机、金额(分)工具
  seed.service.ts               # 用户/商品种子化 + 历史回填 + ID 缓存
  order-generator.service.ts    # 下单生成器（含速率档位）
  status-flow.service.ts        # 订单状态流转器
  canceler.service.ts           # 取消器
  price-adjuster.service.ts     # 调价器
  user-updater.service.ts       # 用户更新器
  deletion-cleaner.service.ts   # 删除清理器
  stats.service.ts              # 事件计数器 + 实时快照
  task.base.ts                  # 循环任务基类
  sim.service.ts                # 编排器（启动/暂停/恢复）
  sim.controller.ts             # HTTP 控制台
  sim.module.ts
```
